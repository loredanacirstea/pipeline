import dT_setTypes from './types';
import dT_setFunctions from './methods';

const dT = dT_setTypes(dT_setFunctions({enums:{controls:{}}, enum_choices:{types:[]}, composites:{}, controls:{tuple:{}, array:{}}, t:{}, settings: {
  refURL: "http://dTypesite.org/package/#"
}}))

export default dT;
